import { SettingRuleEngine } from './setting-rule-engine';

describe('SettingRuleEngine', () => {
  it('should create an instance', () => {
    expect(new SettingRuleEngine()).toBeTruthy();
  });
});
